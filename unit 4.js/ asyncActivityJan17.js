//Using the list above, create a function that will reverse the order of the list/array.

campingSupplies = []

function reverse()
return list.reverse()
const= ['tent','sleeping bag','flash light','camping knife']
 console.log(reversesupplies);= ['tent','sleeping bag','flash light','camping knife']

 // Create a function that will remove the "flash light" item from the list/array and replace it with a "camp fire kit" item.

 function replace(flashlight) {
    const flashlightIndex = array.indexOf("flashlight");
  
    if (flashlightIndex !== -1) {
      array[flashlightIndex] = "campfire kit";
    }
  
    return array;
  }