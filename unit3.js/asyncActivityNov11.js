' The Assignment Operator = assigns values examples '
' == operator is known as the loose equality operator. It compares two values for equality, and it performs type coercion if necessary.'
' the && operator is used to combine two mor more expressions and evaluate true'
' this  || the logical operator this it  It used to perform a logical OR operation on two operands'